pipeline {
    agent any

    tools {
        jdk 'jdk25'
        maven 'Maven'
    }

    stages {

        stage('Checkout') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/kavindideepashika26-max/report.git'
            }
        }

        stage('Build') {
            steps {
                dir('report-api') {
                    bat 'mvn clean package -DskipTests'
                }
            }
        }

        stage('Docker Build') {
            steps {
                dir('report-api') {
                    bat 'docker build -t report-api .'
                }
            }
        }
    }
}